# theme = dark:"Catppuccin Mocha",light:"Catppuccin Latte"
# theme = dark:"Builtin Solarized Dark",light:"Builtin Solarized Light"
theme = "Builtin Solarized Dark"
font-family = "JetBrainsMono Nerd Font Mono"

window-height = 75
window-width = 220
window-theme = system
macos-titlebar-style = tabs 

# should be enabled by default, but i'm being pedantic here
shell-integration = zsh

# quick terminal configuration
keybind = global:ctrl+cmd+grave_accent=toggle_quick_terminal
quick-terminal-position=center

# 20250916(sulrich): the following is only available in ghostty 1.2.0
# 
# ref: https://ghostty.org/docs/install/release-notes/1-2-0#quick-terminal-size
# The primary axis is defined by the quick-terminal-position configuration. For
# the top and bottom values, the primary axis is the height. For the left and
# right values, the primary axis is the width. For center, it depends on your
# monitor orientation: it is height for landscape and width for portrait.
#
#                  primary, secondary - requires units (% or px)
quick-terminal-size = 50%, 50%

# 20250916: the above doesn't seem to be working the way i'd like it to.

## cursor bits
cursor-style = block
cursor-style-blink = false
shell-integration-features = no-cursor

# cursor shaders - 'cause i'm a dork
# note that this only works with the ghostty@tip releases at the moment
# cursor-style = "block"
# cursor-style-blink = true
# custom-shader = "shaders/cursor_frozen.glsl"
keybind = shift+enter=text:\x1b\r
